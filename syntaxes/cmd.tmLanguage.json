{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "CMD",
  "patterns": [
    {
      "include": "#comments"
    },
    {
      "include": "#support"
    },
    {
      "include": "#variables"
    },
    {
      "include": "#constants"
    },
    {
      "include": "#keywords"
    },
    {
      "include": "#strings"
    }
  ],
  "repository": {
    "comments": {
      "patterns": [
        {
          "comment": "Comment. Command definition source uses the CL form /* ... */, which may span lines.",
          "name": "comment.line.cmd",
          "begin": "(\\/\\*)",
          "end": "(\\*\\/)"
        }
      ]
    },
    "constants": {
      "patterns": [
        {
          "comment": "Indicator special values, *IN and *INnn.",
          "name": "constant.language.cmd",
          "match": "(?i)[*]\\b(IN)([0-9]{0,2})\\b"
        },
        {
          "comment": "Numeric literals, such as the length and decimal positions on LEN.",
          "name": "constant.numeric.cmd",
          "match": "(\\b[0-9]+)|([0-9]*[.][0-9]*)"
        },
        {
          "comment": "Special values, written with a leading asterisk.",
          "name": "constant.language.cmd",
          "match": "[*][a-zA-Z][a-zA-Z0-9]*"
        }
      ]
    },
    "keywords": {
      "patterns": [
        {
          "comment": "A statement label. QUAL, ELEM and PMTCTL groups are named by a label and referred to by that name from a PARM.",
          "name": "keyword.control.cmd.label",
          "match": "^\\s*[a-zA-Z_@#$§ÆØÅÄÖ£Ñ¥àÐŞİ][a-zA-Z0-9_@#$§ÆØÅÄÖ£Ñ¥àÐŞİ]*:"
        },
        {
          "comment": "The six command definition statements: CMD, PARM, ELEM, QUAL, DEP and PMTCTL. The guards spell out the characters a name may contain rather than using a word boundary, because the variant characters are not word characters and a boundary would let a statement name match the front of a longer name: QUAL would match the first four characters of qual@#$.",
          "name": "keyword.other.cmd",
          "match": "(?i)(?<![a-zA-Z0-9_@#$§ÆØÅÄÖ£Ñ¥àÐŞİ])(QUAL|PMTCTL|PARM|ELEM|DEP|CMD)(?![a-zA-Z0-9_@#$§ÆØÅÄÖ£Ñ¥àÐŞİ])"
        },
        {
          "comment": "Symbolic operators: concatenation, logical and relational. Written with a leading asterisk to distinguish them from names.",
          "name": "keyword.other.cmd",
          "match": "(?i)[*](CAT|TCAT|BCAT|AND|OR|NOT|EQ|GT|LT|GE|LE|NE|NG|NL)"
        },
        {
          "comment": "Qualified name separator, as in MYLIB/MYPGM or *LIBL/QCMD. Matched before the arithmetic operators so a library qualifier is not read as division. Both sides list the characters a name may contain, the variant ones included, so that a qualified name carrying them still separates.",
          "name": "punctuation.separator.cmd",
          "match": "(?<=[a-zA-Z0-9_@#$§ÆØÅÄÖ£Ñ¥àÐŞİ])\\/(?=[a-zA-Z0-9_@#$§ÆØÅÄÖ£Ñ¥àÐŞİ*&])"
        },
        {
          "comment": "Arithmetic, comparison and concatenation operators written as symbols.",
          "name": "keyword.other.cmd",
          "match": "(([\\|](\\||>|<))|\\+|\\-|\\*|\\/|>=|<=|=|>|<|:)"
        }
      ]
    },
    "strings": {
      "patterns": [
        {
          "comment": "Hexadecimal literal, X'hhhh'.",
          "name": "string.other.cmd.hex",
          "begin": "(?i)x'",
          "end": "'"
        },
        {
          "comment": "Quoted literal, such as prompt text or a special value's replacement.",
          "name": "string.quoted.single.cmd",
          "begin": "'",
          "end": "'",
          "patterns": [
            {
              "comment": "Backslash escape inside a quoted literal. IBM i literals escape an apostrophe by doubling it rather than with a backslash, so this rule appears to be vestigial.",
              "name": "constant.character.escape.cmd",
              "match": "\\\\."
            }
          ]
        }
      ]
    },
    "support": {
      "patterns": [
        {
          "comment": "Parameter keyword, recognised by the parenthesis that follows it: KWD, TYPE, LEN, DFT and the rest.",
          "name": "support.function.cmd",
          "match": "[a-zA-Z_][a-zA-Z0-9_]*(?=\\()"
        }
      ]
    },
    "variables": {
      "patterns": [
        {
          "comment": "A CL variable reference, introduced by &.",
          "name": "variable.parameter.cmd",
          "match": "[&][a-zA-Z_@#$§ÆØÅÄÖ£Ñ¥àÐŞİ][a-zA-Z0-9_@#$§ÆØÅÄÖ£Ñ¥àÐŞİ]*"
        }
      ]
    }
  },
  "scopeName": "source.cmd"
}
