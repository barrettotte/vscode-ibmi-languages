{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "DDS.PF",
  "patterns": [
    {
      "include": "#comments"
    },
    {
      "include": "#gutter"
    },
    {
      "include": "#constants"
    },
    {
      "include": "#keywords"
    },
    {
      "include": "#strings"
    }
  ],
  "repository": {
    "comments": {
      "patterns": [
        {
          "comment": "Comment line: * in column 7. Columns 1-5 are the sequence number and column 6 the form type.",
          "name": "comment.line.dds.pf",
          "match": "^.{5}(.)(\\*).*"
        }
      ]
    },
    "gutter": {
      "patterns": [
        {
          "comment": "Sequence number area, columns 1-5. Not source; scoped as a comment so it recedes visually.",
          "name": "comment.gutter",
          "match": "^.{1,5}"
        },
        {
          "comment": "Anything past column 80 is ignored by the DDS compiler.",
          "name": "comment.block.line.dds",
          "begin": "(?<=^.{80})",
          "end": "\n"
        }
      ]
    },
    "constants": {
      "patterns": [
        {
          "comment": "Special values such as *ISO or *BLANK. Anchored to the functions area (column 45 onward); a special value cannot appear in the name or attribute columns.",
          "name": "constant.language.dds.pf",
          "match": "(?<=(.{44}))(\\*)[a-zA-Z][a-zA-Z0-9]*"
        },
        {
          "comment": "Column 17, the name type. R marks a record format, K a key field. A physical file has no J, S or O entries.",
          "name": "constant.language.dds.pf.nametype",
          "match": "(?i)(?<=^.{5}(A|\\s).{10})(R|K)"
        },
        {
          "comment": "Column 29: R means the field's attributes come from the field reference file named by REF or REFFLD.",
          "name": "constant.language.dds.pf.ref",
          "match": "(?i)(?<=^.{5}(A|\\s).{22})R"
        },
        {
          "comment": "Field length, columns 30-34.",
          "name": "constant.language.dds.pf.len",
          "match": "(?i)(?<=^.{5}(A|\\s).{23})[0-9|\\s]{5}"
        },
        {
          "comment": "Column 35, the data type: P packed, S zoned, B binary, F float, A character, L date, T time, Z timestamp, H hexadecimal, J DBCS-only, E DBCS-either, O DBCS-open, G graphic, 5 unsigned binary.",
          "name": "constant.language.dds.pf.datatype",
          "match": "(?i)(?<=^.{5}(A|\\s).{28})(P|S|B|F|A|L|T|Z|H|J|E|O|G|5)"
        },
        {
          "comment": "Decimal positions, columns 36-37.",
          "name": "constant.language.dds.pf.decpos",
          "match": "(?i)(?<=^.{5}(A|\\s).{29})([0-9]|\\s){2}"
        },
        {
          "comment": "Column 38, field usage. Physical file fields are always both input and output, so B is the only valid entry.",
          "name": "constant.language.dds.pf.use",
          "match": "(?i)(?<=^.{5}(A|\\s).{31})(B)"
        },
        {
          "comment": "EDTCDE edit code. Valid codes are 1-4, A-D, J-Q and W-Z, plus 5-9 for user-defined edit codes.",
          "name": "constant.language.dds.pf.edtcde",
          "match": "(?i)(?<=EDTCDE\\()[1-9A-DJ-QW-Z](?=[\\s)])"
        },
        {
          "comment": "REFSHIFT keyboard shift. A, X, W, N, I, D or M for character fields; S, Y, N, I or D for numeric fields.",
          "name": "constant.language.dds.pf.refshift",
          "match": "(?i)(?<=REFSHIFT\\()[ADIMNSWXY](?=[\\s)])"
        },
        {
          "comment": "Numeric literals in keyword parameters. Anchored to the functions area (column 45 onward), otherwise a digit inside a field name such as #CBX#3 is highlighted as a constant. The length, decimal, indicator and location columns have their own rules.",
          "name": "constant.numeric.dds.pf",
          "match": "(?<=(.{44}))(\\b[0-9]+)|([0-9]*[.][0-9]*)"
        }
      ]
    },
    "keywords": {
      "patterns": [
        {
          "comment": "Column 6, the form type. Always A for DDS.",
          "name": "keyword.other.dds.pf.spec",
          "match": "(?i)(?<=^.{5})(A)"
        },
        {
          "comment": "Continuation character joining this line to the next. Both + and - are valid; + discards the leading blanks of the continuation line and - preserves them. Anchored to end of line so a hyphen or plus inside a keyword parameter is unaffected.",
          "name": "keyword.other.dds.pf",
          "match": "[+-](?=\\s*$)"
        },
        {
          "comment": "Keyword set for the functions area. The first lookbehind requires at least 44 characters before the match, so a keyword is found anywhere from column 45 onward and one line may carry several. The second requires the match to begin exactly at column 45 or to follow a non-word character: a plain \\b is wrong here, because a keyword starting at column 45 is valid even when column 44 holds a digit, as it does when a position entry abuts the keyword. The trailing boundary stops a keyword matching inside a longer name such as MYGDF.",
          "name": "keyword.other.dds.pf.funcs",
          "match": "(?i)(?<=(.{44}))(?<=^.{44}|[^A-Za-z0-9])(ZONE|VARLEN|VALUES|UNSIGNED|SIGNED|UNIQUE|TIMSEP|TIMFMT|TEXT|REFSHIFT|REFFLD|REF|RANGE|NOALTSEQ|LIFO|FORMAT|FLTPCN|FIFO|FCFO|EDTCDE|EDTWRD|DIGIT|DFT|DESCEND|DATSEP|DATFMT|COMP|COLHDG|CMP|CHKMSGID|CHECK|CCSID|ALWNULL|ALTSEQ|ALIAS|ABSVAL)\\b\\b"
        },
        {
          "comment": "Argument block of a COMP or CMP validity-check comparison. A container rule, so it carries no theme colour of its own; the nested rules scope the operator, the value, and any literal.",
          "name": "dds.pf.comp",
          "begin": "(?i)(?<=(.{44}))((?<=((COMP)\\())|(?<=((CMP)\\()))",
          "end": "(?=(\\)))",
          "patterns": [
            {
              "include": "#contline"
            },
            {
              "comment": "Relational operators for COMP and CMP: EQ, NE, LT, NL, GT, NG, LE, GE.",
              "name": "keyword.other.dds.pf.comp.values",
              "match": "(?i)(\\b(GE|LE|NG|GT|NL|LT|NE|EQ)\\b)"
            },
            {
              "comment": "Numeric literals in keyword parameters, such as RANGE(21 65).",
              "name": "constant.numeric.dds.pf",
              "match": "\\b([0-9]+)\\b"
            },
            {
              "include": "#strings"
            }
          ]
        },
        {
          "comment": "Argument block of the CHECK keyword. CHECK accepts a list of edit-check codes, so a lookbehind anchored to CHECK( would only ever match the first one. A container rule carries no theme colour of its own.",
          "name": "dds.pf.check",
          "begin": "(?i)(?<=CHECK\\()",
          "end": "(?=\\))",
          "patterns": [
            {
              "include": "#contline"
            },
            {
              "comment": "CHECK edit-check codes: AB, ME, MF, M10, M11, VN and VNE. M10 and M11 are listed before M1 -prefixed alternatives so the longer code wins.",
              "name": "constant.language.dds.pf.check",
              "match": "(?i)\\b(M10|M11|AB|ME|MF|VNE|VN)\\b"
            }
          ]
        }
      ]
    },
    "strings": {
      "patterns": [
        {
          "comment": "A literal closes on its quote, or at the end of the line, so one missing quote cannot run on through the rest of the file. It does not close where the line ends in a minus or a plus, which the DDS reference defines as continuing the keyword and its parameter values on the next line; a minus resumes at position 45 and a plus at the first non-blank of the functions field. Blanks are excluded from the class because the sign must be the last non-blank character in the field. A comment line is consumed whole by the rule below, which leaves nothing for the end pattern to match, so a comment sitting between two halves of a literal does not close it.",
          "name": "string.other.dds.pf.hex",
          "begin": "(?i)x'",
          "end": "'|[^-+\\s]\\s*$",
          "patterns": [
            {
              "comment": "Continuation character ending an unterminated literal. DDS discards it and joins the lines, so it is not part of the literal's value. Anchored to end of line, so a hyphen or plus inside the text is unaffected.",
              "name": "keyword.other.dds.pf",
              "match": "[+-](?=\\s*$)"
            },
            {
              "comment": "Comment line: * in column 7. Columns 1-5 are the sequence number and column 6 the form type.",
              "name": "comment.line.dds.pf",
              "match": "^.{5}(.)(\\*).*"
            },
            {
              "comment": "Sequence number area, columns 1-5. Not source; scoped as a comment so it recedes visually.",
              "name": "comment.gutter",
              "match": "^.{1,5}"
            },
            {
              "comment": "Column 6, the form type. Always A for DDS.",
              "name": "keyword.other.dds.pf.spec",
              "match": "(?i)(?<=^.{5})(A)"
            }
          ]
        },
        {
          "comment": "A literal closes on its quote, or at the end of the line, so one missing quote cannot run on through the rest of the file. It does not close where the line ends in a minus or a plus, which the DDS reference defines as continuing the keyword and its parameter values on the next line; a minus resumes at position 45 and a plus at the first non-blank of the functions field. Blanks are excluded from the class because the sign must be the last non-blank character in the field. A comment line is consumed whole by the rule below, which leaves nothing for the end pattern to match, so a comment sitting between two halves of a literal does not close it.",
          "name": "string.quoted.single.dds.pf",
          "begin": "'",
          "end": "'|[^-+\\s]\\s*$",
          "patterns": [
            {
              "comment": "Continuation character ending an unterminated literal. DDS discards it and joins the lines, so it is not part of the literal's value. Anchored to end of line, so a hyphen or plus inside the text is unaffected.",
              "name": "keyword.other.dds.pf",
              "match": "[+-](?=\\s*$)"
            },
            {
              "comment": "Comment line: * in column 7. Columns 1-5 are the sequence number and column 6 the form type.",
              "name": "comment.line.dds.pf",
              "match": "^.{5}(.)(\\*).*"
            },
            {
              "comment": "Sequence number area, columns 1-5. Not source; scoped as a comment so it recedes visually.",
              "name": "comment.gutter",
              "match": "^.{1,5}"
            },
            {
              "comment": "Column 6, the form type. Always A for DDS.",
              "name": "keyword.other.dds.pf.spec",
              "match": "(?i)(?<=^.{5})(A)"
            }
          ]
        }
      ]
    },
    "contline": {
      "patterns": [
        {
          "include": "#comments"
        },
        {
          "include": "#gutter"
        },
        {
          "comment": "The form type in position 6, repeated here because a keyword whose values are continued carries one on every line it spans.",
          "name": "keyword.other.dds.pf.spec",
          "match": "(?i)(?<=^.{5})(A)"
        }
      ]
    }
  },
  "scopeName": "source.dds.pf"
}
