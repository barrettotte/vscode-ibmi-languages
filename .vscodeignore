# Allowlist. vsce packages the working directory and reads only this file --
# Ignore everything, then re-include exactly what the extension needs.

**

!package.json
!README.md
!CHANGELOG.md
!LICENSE
!images/icon.png
!syntaxes/*.tmLanguage.json
!configurations/*.language-configuration.json
